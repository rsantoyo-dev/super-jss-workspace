import { SuperJssDirective } from './super-jss.directive';

describe('SuperJssDirective', () => {
  it('should create an instance', () => {
    const directive = new SuperJssDirective();
    expect(directive).toBeTruthy();
  });
});
